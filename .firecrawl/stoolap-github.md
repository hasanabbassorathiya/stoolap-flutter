[Skip to content](https://github.com/stoolap/stoolap#start-of-content)

## Navigation Menu

Toggle navigation

[Homepage](https://github.com/)

[Sign in](https://github.com/login?return_to=https%3A%2F%2Fgithub.com%2Fstoolap%2Fstoolap)

Appearance settings

- Platform



  - AI CODE CREATION

    - [GitHub CopilotWrite better code with AI](https://github.com/features/copilot)
    - [GitHub SparkBuild and deploy intelligent apps](https://github.com/features/spark)
    - [GitHub ModelsManage and compare prompts](https://github.com/features/models)
    - [MCP RegistryNewIntegrate external tools](https://github.com/mcp)

  - DEVELOPER WORKFLOWS

    - [ActionsAutomate any workflow](https://github.com/features/actions)
    - [CodespacesInstant dev environments](https://github.com/features/codespaces)
    - [IssuesPlan and track work](https://github.com/features/issues)
    - [Code ReviewManage code changes](https://github.com/features/code-review)

  - APPLICATION SECURITY

    - [GitHub Advanced SecurityFind and fix vulnerabilities](https://github.com/security/advanced-security)
    - [Code securitySecure your code as you build](https://github.com/security/advanced-security/code-security)
    - [Secret protectionStop leaks before they start](https://github.com/security/advanced-security/secret-protection)

  - EXPLORE

    - [Why GitHub](https://github.com/why-github)
    - [Documentation](https://docs.github.com/)
    - [Blog](https://github.blog/)
    - [Changelog](https://github.blog/changelog)
    - [Marketplace](https://github.com/marketplace)

[View all features](https://github.com/features)

- Solutions



  - BY COMPANY SIZE

    - [Enterprises](https://github.com/enterprise)
    - [Small and medium teams](https://github.com/team)
    - [Startups](https://github.com/enterprise/startups)
    - [Nonprofits](https://github.com/solutions/industry/nonprofits)

  - BY USE CASE

    - [App Modernization](https://github.com/solutions/use-case/app-modernization)
    - [DevSecOps](https://github.com/solutions/use-case/devsecops)
    - [DevOps](https://github.com/solutions/use-case/devops)
    - [CI/CD](https://github.com/solutions/use-case/ci-cd)
    - [View all use cases](https://github.com/solutions/use-case)

  - BY INDUSTRY

    - [Healthcare](https://github.com/solutions/industry/healthcare)
    - [Financial services](https://github.com/solutions/industry/financial-services)
    - [Manufacturing](https://github.com/solutions/industry/manufacturing)
    - [Government](https://github.com/solutions/industry/government)
    - [View all industries](https://github.com/solutions/industry)

[View all solutions](https://github.com/solutions)

- Resources



  - EXPLORE BY TOPIC

    - [AI](https://github.com/resources/articles?topic=ai)
    - [Software Development](https://github.com/resources/articles?topic=software-development)
    - [DevOps](https://github.com/resources/articles?topic=devops)
    - [Security](https://github.com/resources/articles?topic=security)
    - [View all topics](https://github.com/resources/articles)

  - EXPLORE BY TYPE

    - [Customer stories](https://github.com/customer-stories)
    - [Events & webinars](https://github.com/resources/events)
    - [Ebooks & reports](https://github.com/resources/whitepapers)
    - [Business insights](https://github.com/solutions/executive-insights)
    - [GitHub Skills](https://skills.github.com/)

  - SUPPORT & SERVICES

    - [Documentation](https://docs.github.com/)
    - [Customer support](https://support.github.com/)
    - [Community forum](https://github.com/orgs/community/discussions)
    - [Trust center](https://github.com/trust-center)
    - [Partners](https://github.com/partners)

[View all resources](https://github.com/resources)

- Open Source



  - COMMUNITY

    - [GitHub SponsorsFund open source developers](https://github.com/sponsors)

  - PROGRAMS

    - [Security Lab](https://securitylab.github.com/)
    - [Maintainer Community](https://maintainers.github.com/)
    - [Accelerator](https://github.com/accelerator)
    - [GitHub Stars](https://stars.github.com/)
    - [Archive Program](https://archiveprogram.github.com/)

  - REPOSITORIES

    - [Topics](https://github.com/topics)
    - [Trending](https://github.com/trending)
    - [Collections](https://github.com/collections)

- Enterprise



  - ENTERPRISE SOLUTIONS

    - [Enterprise platformAI-powered developer platform](https://github.com/enterprise)

  - AVAILABLE ADD-ONS

    - [GitHub Advanced SecurityEnterprise-grade security features](https://github.com/security/advanced-security)
    - [Copilot for BusinessEnterprise-grade AI features](https://github.com/features/copilot/copilot-business)
    - [Premium SupportEnterprise-grade 24/7 support](https://github.com/premium-support)

- [Pricing](https://github.com/pricing)

Search or jump to...

# Search code, repositories, users, issues, pull requests...

Search


Clear

[Search syntax tips](https://docs.github.com/search-github/github-code-search/understanding-github-code-search-syntax)

# Provide feedback

We read every piece of feedback, and take your input very seriously.

Include my email address so I can be contacted

Cancel
Submit feedback

# Saved searches

## Use saved searches to filter your results more quickly

Name

Query

To see all available qualifiers, see our [documentation](https://docs.github.com/search-github/github-code-search/understanding-github-code-search-syntax).


Cancel
Create saved search

[Sign in](https://github.com/login?return_to=https%3A%2F%2Fgithub.com%2Fstoolap%2Fstoolap)

[Sign up](https://github.com/signup?ref_cta=Sign+up&ref_loc=header+logged+out&ref_page=%2F%3Cuser-name%3E%2F%3Crepo-name%3E&source=header-repo&source_repo=stoolap%2Fstoolap)

Appearance settings

Resetting focus

You signed in with another tab or window. [Reload](https://github.com/stoolap/stoolap) to refresh your session.You signed out in another tab or window. [Reload](https://github.com/stoolap/stoolap) to refresh your session.You switched accounts on another tab or window. [Reload](https://github.com/stoolap/stoolap) to refresh your session.Dismiss alert

{{ message }}

[stoolap](https://github.com/stoolap)/ **[stoolap](https://github.com/stoolap/stoolap)** Public

- [Notifications](https://github.com/login?return_to=%2Fstoolap%2Fstoolap) You must be signed in to change notification settings
- [Fork\\
43](https://github.com/login?return_to=%2Fstoolap%2Fstoolap)
- [Star\\
1.2k](https://github.com/login?return_to=%2Fstoolap%2Fstoolap)


- [Code](https://github.com/stoolap/stoolap)
- [Issues6](https://github.com/stoolap/stoolap/issues)
- [Pull requests2](https://github.com/stoolap/stoolap/pulls)
- [Discussions](https://github.com/stoolap/stoolap/discussions)
- [Actions](https://github.com/stoolap/stoolap/actions)
- [Projects](https://github.com/stoolap/stoolap/projects)
- [Security and quality0](https://github.com/stoolap/stoolap/security)
- [Insights](https://github.com/stoolap/stoolap/pulse)

Additional navigation options

- [Code](https://github.com/stoolap/stoolap)
- [Issues](https://github.com/stoolap/stoolap/issues)
- [Pull requests](https://github.com/stoolap/stoolap/pulls)
- [Discussions](https://github.com/stoolap/stoolap/discussions)
- [Actions](https://github.com/stoolap/stoolap/actions)
- [Projects](https://github.com/stoolap/stoolap/projects)
- [Security and quality](https://github.com/stoolap/stoolap/security)
- [Insights](https://github.com/stoolap/stoolap/pulse)

# stoolap/stoolap

main

[**3** Branches](https://github.com/stoolap/stoolap/branches) [**14** Tags](https://github.com/stoolap/stoolap/tags)

[Go to Branches page](https://github.com/stoolap/stoolap/branches)[Go to Tags page](https://github.com/stoolap/stoolap/tags)

Go to file

Code

Open more actions menu

## Folders and files

| Name | Name | Last commit message | Last commit date |
| --- | --- | --- | --- |
| ## Latest commit<br>[![semihalev](https://avatars.githubusercontent.com/u/539588?v=4&size=40)](https://github.com/semihalev)[semihalev](https://github.com/stoolap/stoolap/commits?author=semihalev)<br>[Revert "feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN…](https://github.com/stoolap/stoolap/commit/7e6634ba8447f33ae99aefb99693459cd691394a)<br>Open commit detailssuccess<br>3 weeks agoApr 22, 2026<br>[7e6634b](https://github.com/stoolap/stoolap/commit/7e6634ba8447f33ae99aefb99693459cd691394a) · 3 weeks agoApr 22, 2026<br>## History<br>[191 Commits](https://github.com/stoolap/stoolap/commits/main/) <br>Open commit details<br>[View commit history for this file.](https://github.com/stoolap/stoolap/commits/main/) 191 Commits |
| [.cargo](https://github.com/stoolap/stoolap/tree/main/.cargo ".cargo") | [.cargo](https://github.com/stoolap/stoolap/tree/main/.cargo ".cargo") | [ci: exclude fast-path and optimizer functions from mutation testing](https://github.com/stoolap/stoolap/commit/a63f1820b1388b211b9ad29c4a0c7a60c9c73980 "ci: exclude fast-path and optimizer functions from mutation testing  These 28 patterns cover functions where mutations cannot produce observable differences in test output: optimizer heuristics that choose between equivalent strategies, and try_*/fast_* functions that return None on failure and fall back to the correct slow path. Reduces mutant count from 1764 to 1108 across the targeted files.") | 2 months agoMar 3, 2026 |
| [.github/workflows](https://github.com/stoolap/stoolap/tree/main/.github/workflows "This path skips through empty directories") | [.github/workflows](https://github.com/stoolap/stoolap/tree/main/.github/workflows "This path skips through empty directories") | [ci: rotate Miri module groups, fix mutation shard coverage](https://github.com/stoolap/stoolap/commit/e81bb953f12438557ddfe3880a405be346a7b5bf "ci: rotate Miri module groups, fix mutation shard coverage  Mutation: reduce from 96 to 24 shards (full coverage in 6 weeks instead of ~6 months). Use epoch_day counter instead of day-of-year to avoid skipping shards on non-mutation weekdays.  Miri: rotate through 5 module groups instead of running all 320 tests:   0: compact_arc + compact_vec + buffer_pool   1: cow_hashmap + i64_map (recent Stacked Borrows fixes)   2: cow_btree (excluding deep-tree)   3: core:: (value, row, schema, error)   4: cow_btree deep-tree only (slowest, 5-10 min each) Full Miri cycle completes in ~2 weeks at 3 days/week.") | 2 months agoMar 3, 2026 |
| [benches](https://github.com/stoolap/stoolap/tree/main/benches "benches") | [benches](https://github.com/stoolap/stoolap/tree/main/benches "benches") | [perf: introduce SmartString, I64Map, and value interning for memory e…](https://github.com/stoolap/stoolap/commit/221606b71d2618b99abf95ba77e8c8b479c47256 "perf: introduce SmartString, I64Map, and value interning for memory efficiency (#10)  Major optimizations:  **SmartString** - Custom SSO string replacing CompactString - Inline storage for strings ≤22 bytes (no heap allocation) - Owned (Box<str>) for computed values, Shared (Arc<str>) for cloned values - 24-byte size with O(1) clone for shared variant  **I64Map** - High-performance hashmap for i64 keys - Uses i64::MIN as empty sentinel (row/txn IDs are always ≥0) - FxHash-based with linear probing and backward-shift deletion - ~45% faster lookups than FxHashMap<i64, V>  **Value Interning** - Shared instances for common values - Interned: NULL (all 7 DataTypes), booleans, integers 0-1000 - Reduces allocations for frequently used values in rows  **Aggregation Fixes & Optimizations** - Fixed hash collision bug in single-column GROUP BY (was using hash as key) - Added 2-column tuple optimization (30% faster than Vec<Value>) - Single-column primitive GROUP BY uses I64Map directly  **SIMD Pattern Matching** - memchr::memmem for LIKE '%pattern%' substring search - Pre-compiled Finder stored in CompiledPattern  **Other Improvements** - CompactVec: new insert(), retain(), drain() methods - All indexes migrated to I64Map and CompactVec - Thorough SAFETY documentation on unsafe code") | 4 months agoJan 17, 2026 |
| [docs](https://github.com/stoolap/stoolap/tree/main/docs "docs") | [docs](https://github.com/stoolap/stoolap/tree/main/docs "docs") | [Revert "feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN…](https://github.com/stoolap/stoolap/commit/7e6634ba8447f33ae99aefb99693459cd691394a "Revert \"feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN flag, CLI flag) (#26)\"  This reverts commit 46bd2280cd3612a63624339ce3a67b2fda83c30f.") | 3 weeks agoApr 22, 2026 |
| [examples](https://github.com/stoolap/stoolap/tree/main/examples "examples") | [examples](https://github.com/stoolap/stoolap/tree/main/examples "examples") | [feat: immutable volume storage engine (v0.4.0) (](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") [#20](https://github.com/stoolap/stoolap/pull/20) [)](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") | last monthApr 1, 2026 |
| [fuzz](https://github.com/stoolap/stoolap/tree/main/fuzz "fuzz") | [fuzz](https://github.com/stoolap/stoolap/tree/main/fuzz "fuzz") | [release: v0.3.1 — cached plan API, Node.js driver docs, WASM update](https://github.com/stoolap/stoolap/commit/f8906a3799dbcbdab43f58e46c86364dad23c2a2 "release: v0.3.1 — cached plan API, Node.js driver docs, WASM update  - Add `Database::cached_plan()` / `execute_plan()` / `query_plan()` for pre-parsed SQL reuse - Add `Executor::get_or_create_plan()` and `execute_with_cached_plan()` backend - Re-export `CachedPlanRef` and `ParamVec` from lib.rs - Add \"Drivers\" category and Node.js driver documentation to the website - Bump version to 0.3.1 across Cargo.toml, Cargo.lock, fuzz/Cargo.lock, BENCHMARKS.md, wasm.rs - Rebuild WASM artifacts for v0.3.1") | 3 months agoFeb 18, 2026 |
| [include](https://github.com/stoolap/stoolap/tree/main/include "include") | [include](https://github.com/stoolap/stoolap/tree/main/include "include") | [docs: add batch execution to C API, fix Swift icon and driver ordering](https://github.com/stoolap/stoolap/commit/76bdd894e3b3b802894a0d0f85bdd258652621f2 "docs: add batch execution to C API, fix Swift icon and driver ordering  Add stoolap_stmt_exec_batch documentation to C API driver docs. Replace Swift icon with bird-only version from Wikimedia SVG. Fix duplicate driver order values and reorder sidebar by popularity.") | last monthApr 11, 2026 |
| [src](https://github.com/stoolap/stoolap/tree/main/src "src") | [src](https://github.com/stoolap/stoolap/tree/main/src "src") | [Revert "feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN…](https://github.com/stoolap/stoolap/commit/7e6634ba8447f33ae99aefb99693459cd691394a "Revert \"feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN flag, CLI flag) (#26)\"  This reverts commit 46bd2280cd3612a63624339ce3a67b2fda83c30f.") | 3 weeks agoApr 22, 2026 |
| [tests](https://github.com/stoolap/stoolap/tree/main/tests "tests") | [tests](https://github.com/stoolap/stoolap/tree/main/tests "tests") | [Revert "feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN…](https://github.com/stoolap/stoolap/commit/7e6634ba8447f33ae99aefb99693459cd691394a "Revert \"feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN flag, CLI flag) (#26)\"  This reverts commit 46bd2280cd3612a63624339ce3a67b2fda83c30f.") | 3 weeks agoApr 22, 2026 |
| [.gitignore](https://github.com/stoolap/stoolap/blob/main/.gitignore ".gitignore") | [.gitignore](https://github.com/stoolap/stoolap/blob/main/.gitignore ".gitignore") | [docs: add Development category with testing, limitations, building, a…](https://github.com/stoolap/stoolap/commit/738ae7ea7cafed4b071a7f8c133d25e8020f1e51 "docs: add Development category with testing, limitations, building, and contributing guides  Add four new documentation pages under the Development category: - Testing: test infrastructure, categories, CI/CD pipeline, how to write tests - Known Limitations: consolidated limitations from all feature docs - Building from Source: prerequisites, feature flags, WASM, cross-compilation - Contributing: development rules, coding standards, PR workflow  Update CONTRIBUTING.md with accurate tooling (cargo nextest, clippy flags). Clean up .gitignore entries.") | 2 months agoMar 3, 2026 |
| [BENCHMARKS.md](https://github.com/stoolap/stoolap/blob/main/BENCHMARKS.md "BENCHMARKS.md") | [BENCHMARKS.md](https://github.com/stoolap/stoolap/blob/main/BENCHMARKS.md "BENCHMARKS.md") | [feat: immutable volume storage engine (v0.4.0) (](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") [#20](https://github.com/stoolap/stoolap/pull/20) [)](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") | last monthApr 1, 2026 |
| [CONTRIBUTING.md](https://github.com/stoolap/stoolap/blob/main/CONTRIBUTING.md "CONTRIBUTING.md") | [CONTRIBUTING.md](https://github.com/stoolap/stoolap/blob/main/CONTRIBUTING.md "CONTRIBUTING.md") | [docs: add Development category with testing, limitations, building, a…](https://github.com/stoolap/stoolap/commit/738ae7ea7cafed4b071a7f8c133d25e8020f1e51 "docs: add Development category with testing, limitations, building, and contributing guides  Add four new documentation pages under the Development category: - Testing: test infrastructure, categories, CI/CD pipeline, how to write tests - Known Limitations: consolidated limitations from all feature docs - Building from Source: prerequisites, feature flags, WASM, cross-compilation - Contributing: development rules, coding standards, PR workflow  Update CONTRIBUTING.md with accurate tooling (cargo nextest, clippy flags). Clean up .gitignore entries.") | 2 months agoMar 3, 2026 |
| [Cargo.lock](https://github.com/stoolap/stoolap/blob/main/Cargo.lock "Cargo.lock") | [Cargo.lock](https://github.com/stoolap/stoolap/blob/main/Cargo.lock "Cargo.lock") | [feat: add COPY FROM for CSV and JSON bulk import, fix -- comment parsing](https://github.com/stoolap/stoolap/commit/f0119618818d717ef48b011ca9b30ba3a8c4a0cc "feat: add COPY FROM for CSV and JSON bulk import, fix -- comment parsing  Add COPY FROM statement for fast bulk data import from CSV and JSON files, bypassing per-row SQL parsing. 5x faster than auto-commit INSERT with 8x fewer allocations.  CSV path parses fields directly into target types (no intermediate Value::text allocation). JSON path streams objects via serde_json StreamDeserializer with a custom JsonArrayStripper reader that transforms array syntax into top-level values for O(object) memory on both JSON arrays and JSON Lines.  Includes type coercion validation (rejects silent null on failed conversion), vector dimension checking, Unicode case-insensitive JSON key matching, CHECK constraint and foreign key validation.  Also fixes -- (double dash) to always start a line comment per SQL:2023 section 5.2. Double negation must now use `- -val` with a space.  Syntax:   COPY table [(cols)] FROM 'path' [WITH (FORMAT CSV|JSON, HEADER bool,     DELIMITER 'c', NULL 'str')]  31 integration tests covering both formats, error cases, type coercion, vector dimensions, case-insensitive keys, and large data.") | last monthApr 10, 2026 |
| [Cargo.toml](https://github.com/stoolap/stoolap/blob/main/Cargo.toml "Cargo.toml") | [Cargo.toml](https://github.com/stoolap/stoolap/blob/main/Cargo.toml "Cargo.toml") | [Revert "feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN…](https://github.com/stoolap/stoolap/commit/7e6634ba8447f33ae99aefb99693459cd691394a "Revert \"feat(api): add read-only mode (Database/ReadOnlyDatabase, DSN flag, CLI flag) (#26)\"  This reverts commit 46bd2280cd3612a63624339ce3a67b2fda83c30f.") | 3 weeks agoApr 22, 2026 |
| [LICENSE](https://github.com/stoolap/stoolap/blob/main/LICENSE "LICENSE") | [LICENSE](https://github.com/stoolap/stoolap/blob/main/LICENSE "LICENSE") | [Initial release: Stoolap - A Modern Embedded SQL Database in Pure Rust](https://github.com/stoolap/stoolap/commit/768eb836de0ff072b81438a9f9c55a7f93b0cb85 "Initial release: Stoolap - A Modern Embedded SQL Database in Pure Rust  Stoolap is a high-performance embedded SQL database featuring:  Core Features: - Full ACID transactions with MVCC (READ COMMITTED & SNAPSHOT isolation) - Cost-based query optimizer with adaptive execution - Parallel query execution via Rayon - 101+ built-in functions (string, math, date/time, JSON, aggregate, window) - Multiple index types: B-tree, Hash, Bitmap (auto-selected or explicit) - Multi-column composite indexes - WAL + snapshots with crash recovery  SQL Support: - JOINs (INNER, LEFT, RIGHT, FULL OUTER, CROSS) - Subqueries (scalar, IN, EXISTS, correlated) - Common Table Expressions (WITH and WITH RECURSIVE) - Window functions (ROW_NUMBER, RANK, LAG, LEAD, etc.) - ROLLUP, CUBE, GROUPING SETS - Temporal queries (AS OF TIMESTAMP/TRANSACTION) - Views, RETURNING clause, ON DUPLICATE KEY UPDATE  104K lines of Rust | No C dependencies | Full documentation at stoolap.io") | 5 months agoDec 11, 2025 |
| [README.md](https://github.com/stoolap/stoolap/blob/main/README.md "README.md") | [README.md](https://github.com/stoolap/stoolap/blob/main/README.md "README.md") | [feat: immutable volume storage engine (v0.4.0) (](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") [#20](https://github.com/stoolap/stoolap/pull/20) [)](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") | last monthApr 1, 2026 |
| [build.rs](https://github.com/stoolap/stoolap/blob/main/build.rs "build.rs") | [build.rs](https://github.com/stoolap/stoolap/blob/main/build.rs "build.rs") | [feat: immutable volume storage engine (v0.4.0) (](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") [#20](https://github.com/stoolap/stoolap/pull/20) [)](https://github.com/stoolap/stoolap/commit/9134f4413a7aa2b7b1bf29f359fd121aae835e52 "feat: immutable volume storage engine (v0.4.0) (#20)  * feat: hot/cold volume storage engine replacing snapshot persistence  Introduce a columnar immutable volume system where each table is split into a hot MVCC buffer (recent writes, WAL-backed) and cold frozen volumes (historical data, column-major with zone maps, bloom filters, dictionary encoding, and CRC32 integrity). The query engine merges both sources transparently through the existing Table/Scanner traits.  Volume layer (new src/storage/volume/, ~10K LOC): - column.rs: typed column arrays (i64/f64/bool/string/timestamp/bytes)   with null bitmaps, dictionary encoding for text, bloom filters - writer.rs: VolumeBuilder accumulates rows, FrozenVolume is the   immutable read structure with per-volume lazy hash indexes - format.rs: binary serialization with CRC32, version header - scanner.rs: VolumeScanner with skip sets, MergingScanner chains   hot+cold sources, schema normalization for ALTER TABLE columns - manifest.rs: SegmentManager owns per-table manifest (segment list,   versioned tombstones with commit_seq), atomic segment swap - table.rs: SegmentedTable wraps MVCCTable with cold scan merging,   aggregation pushdowns (sum/min/max/count from pre-computed stats),   partition helpers, unique constraint checking across hot+cold - seal.rs: extract hot rows into FrozenVolume + write to disk - stats.rs: per-column aggregate statistics computed at freeze time - io.rs: volume file I/O with fsync-before-rename crash safety  Engine changes (src/storage/mvcc/engine.rs): - Checkpoint cycle: seal hot to volumes, persist manifests, truncate WAL - Size-sorted compaction: merge smallest volumes first, bounded I/O,   dynamic cooldown, snapshot-safe guard, atomic segment swap - Seal safety: extraction snapshot detects concurrent commits, skipped   rows get tombstoned for recovery and row count correctness - WAL recovery: volumes loaded before replay, idempotent insert skip - Backup snapshots: PRAGMA SNAPSHOT/RESTORE with manifest-based   coherent selection, per-timestamp DDL files, CLI restore - Automatic migration from v0.3.7 snapshot format to volumes  MVCC changes: - SegmentedTable as Table trait impl (transparent to executor) - Seal fence: RwLock where commits hold shared, checkpoint holds   exclusive briefly (no disk I/O inside fence) - ExtractionSnapshot: CowBTree clone at seal time, txn_id comparison   in remove_sealed_rows prevents discarding concurrent updates - track_external_claim: cold row claims added to write_set so   commit/rollback releases them (prevents permanent row blocking) - Sub-batched remove_sealed_rows: 2K chunks with lock release between - Per-table upsert mutex for ON CONFLICT serialization - SealFenceGuard: RAII wrapper for parking_lot raw read lock  Config changes: - snapshot_interval renamed to checkpoint_interval (default 60s) - New: compact_threshold (default 4), checkpoint_on_close, keep_snapshots - Legacy DSN parameter names still accepted  Schema evolution on cold data: - Aggregation pushdowns (sum/min/max) use column defaults for old volumes - Unique enforcement uses defaults for missing columns - Partition helpers include old volumes with default matching - get_partition_count/values bail when hot has no index  Other: - CowBTree MIN_KEYS: (MAX_KEYS-1)/2 for correct merge invariant - 4811 tests pass  * feat: adaptive compaction with background thread and incremental dedup  Compaction algorithm redesign with three phases: 1. Convergence: merge (excess + 1) volumes per cycle to reach threshold 2. Opportunistic: sweep similarly-sized volumes below running average 3. Incremental dedup: when accumulated rows exceed 25% of largest volume,    include it in merge to eliminate duplicate rows from upserts  Background compaction thread: - Compaction spawned on std::thread after checkpoint (not blocking next cycle) - AtomicBool CAS for mutual exclusion (no concurrent compaction) - AtomicBoolGuard RAII for panic-safe flag cleanup - PRAGMA CHECKPOINT/close/restore wait for background compaction via CAS loop - PRAGMA SNAPSHOT blocks compaction for consistent cold-state backup  Tombstone safety for concurrent seal + compaction: - Snapshot-aware cleanup: remove_tombstones_matching_snapshot() only clears   tombstones whose (row_id, commit_seq) matches the compaction snapshot - Seal tombstones use unique seal_seq from registry.get_current_sequence()   so repeated seal-skip tombstones are distinguishable across cycles - add_tombstones() updates existing entries when commit_seq changes - Stale tombstones cleared BEFORE segment registration (not after) to   prevent compaction from seeing new segment with stale tombstones  * fix: precomputed column mapping, column pruning, and compaction TOCTOU race  Replace per-row needs_normalize + get_row_normalized with precomputed ColumnMapping (computed once per volume per scan). Adds column pruning in cold scans via needed_cols bitmask from filter column extraction.  Fix TOCTOU race in SegmentManager where manifest seg_ids and segments map were read under separate locks, allowing compaction's atomic swap to make rows temporarily invisible to unique constraint checks.  - ColSource/ColumnMapping types with compute_column_mapping() in writer.rs - collect_column_indices() on Expression trait for filter column extraction - get_row_needed/get_row_mapped_needed for pruned materialization - Pointer-cached mapping in row-id batch paths (update, collect, fetch) - Partition functions read values through mapping (DROP COLUMN safe) - 9 manifest.rs functions: atomic seg_ids + segments snapshot under   manifest read lock (same ordering as writer, no deadlock risk)  * feat: LZ4 compression for cold volume files  Volumes are LZ4-compressed at the I/O boundary (STVZ magic prefix). Transparent to the format module: serialize produces raw STVL bytes, io.rs compresses on write and decompresses on read.  Backward compatible: old STVL files read directly, new STVZ files decompressed transparently. Compaction naturally rewrites old files.  - volume_compression DSN parameter (on/off, default on) - compression=on/off controls both WAL and volume compression - Size check includes 4-byte magic header to avoid expansion - Drop raw buffer before building compressed output (peak memory) - Update README with volume storage architecture and DSN reference  * feat: row-group zone maps, epoch-based compaction staleness, compaction trigger fix  ## Row-group zone maps - Split volumes into 64K-row logical groups with per-group min/max zone maps - Scanner skips entire groups when filter predicates prove no match - v2 files get row groups computed at load time (bloom filter pattern) - v3 files read pre-serialized row groups from disk - NaN-safe float handling in zone_map_for_range()  ## Epoch-based compaction staleness - Global compaction_epoch increments each cycle - New segments record current epoch in SegmentMeta (in-memory, not persisted) - Phase 4: force-include all volumes when any segment age exceeds compact_threshold * 2 - Prevents large base volumes from accumulating unbounded staleness  ## Compaction trigger fix - Changed >= to > so tables at exactly compact_threshold don't loop  * feat: test-filedb feature, sync_mode=normal fix, benchmark --filedb support  ## test-filedb feature - Routes memory:// databases to temp file-backed databases - All 4811 tests pass with `cargo nextest run --features test-filedb` - Temp directories auto-deleted on Database drop  ## sync_mode=normal fix - Changed from fsync-per-commit to fsync every 1 second - Previous behavior made normal identical to full for auto-commit workloads - Now comparable to SQLite WAL + synchronous=NORMAL - DDL operations still fsync immediately  ## Benchmark --filedb support - Added --filedb flag to benchmark, benchmark_sqlite, benchmark_duckdb - Stoolap uses sync_mode=normal, SQLite uses WAL + synchronous=NORMAL  ## Other fixes - Compaction trigger: >= to > (prevents infinite loop at threshold) - Updated docs for sync mode behavior and DSN parameters  * fix: seal fence for DML/seal race, commit-time cold revalidation  ## Seal fence (per-table RwLock) - DML (insert/update/delete/truncate): shared read fence - Seal: exclusive write fence around register + remove + index cleanup + tombstone cleanup - Prevents race where INSERT's cold check and hot insert see inconsistent state - Manifest persist and file deletion after fence release (minimal DML stall)  ## Commit-time cold revalidation - Records min(seal_generation) per txn per table during DML (only when segments exist) - At commit: always take fence if manager exists, revalidate cold if generation changed - INSERT: checks PK + UNIQUE against cold - UPDATE: checks PK only if changed, UNIQUE only if indexed columns changed - Cold-row claims classified via pending tombstone check (not write_set presence) - Precomputed defaults per-index, falls back to live segment_managers for first-seal - Generation cleanup on all paths (commit, rollback, partial failure, direct Table ops)  ## Index cleanup optimization - Batched per-index via remove_batch_slice, indexes lock dropped early  * chore: bump version to 0.4.0, perf optimizations, LIMIT pushdown fix  - Bump version 0.3.7 -> 0.4.0 across Cargo.toml, README, docs, WASM,   C header, and benchmarks - Scanner: extract duplicated skip/materialize logic into shared inline   helpers, cache row-group boundary to eliminate per-row division - Compaction cooldown: Mutex<Instant> -> AtomicU64 (lock-free) - Bloom filter: zero-alloc add_extension_noop() for Extension types,   backward-compatible tag-0 hash for existing persisted volumes - WASM: gate unused imports and dead code behind cfg(not(wasm32)) - LIMIT pushdown: fix ordered mixed hot/cold returning hot rows first.   Authority map with has_row_id() B-tree point lookups (no O(hot) scan),   oldest-first iteration with early termination, deferred hot   materialization. Unordered path uses limit-aware hot scan. - Update migration guide with v0.4.0 architecture - Remove dataloss_repro_test (superseded by insert_select_dataloss_test)  * feat: V4 compressed columnar format, ColdSegment mapping, schema evolution  V4 on-disk format (STV4) with per-column per-row-group LZ4 compression, streaming CRC32 verification, and u64 block index. Volumes load eagerly from V4 files (matching V3 performance) with CompressedBlockStore available for future column eviction.  Column mapping refactor: - ColdSegment stores ColumnMapping alongside the volume (one source of truth) - Mappings computed once at registration, recomputed on ALTER TABLE - schema_version tracking for correct DROP+ADD column semantics (manifest V6) - Rename-aware mapping via column_renames in compute_column_mapping_with_drops - used_vol_indices prevents two schema columns binding the same physical column - Removed column_aliases, has_aliases, cached_col_remap from FrozenVolume - Removed VolumeGroupAccess (144 lines of indirection) - All scan/DML paths use pre-computed ColdSegment.mapping directly  Performance optimizations: - Pre-build unique hash index at seal/compaction (eliminates first-query stall) - ColdSnapshot for batch constraint checking (one lock acquisition per batch) - SmallVec for per-call allocations (seg_ids, bloom_hashes, col_names) - Arc<[SmartString]> for ColumnData::Dictionary (zero-copy group decompression) - col_dicts on CompressedBlockStore (no dictionary duplication) - Parallel column compression with rayon (--features parallel) - is_eager AtomicBool auto-flips when all OnceLock columns loaded - Dual-case column_name_map (eliminates to_lowercase heap alloc) - CompressedBlockStore dict_lookup for collect_cold_rows text equality  * perf: allocation optimization, visibility bitmap, sorted unique index  - Row reuse in DELETE: reusable Row buffer instead of Vec<Value> per row - LZ4 buffer reuse: decompress_into with shared scratch buffer across groups - decompress_block_into: direct-append to output vectors, no intermediate ColumnData - Pre-sized FxHashSets with estimated capacity across 18 call sites - Hoisted upsert expression compilation out of per-row loop (CompiledUpsert) - Visibility bitmap: pre-computed per-volume bitset at seal/compact for O(1) row dedup - Sorted unique index: Vec<(u64, u32)> with binary search replaces FxHashMap<u64, Vec<u32>> - Bitmap-based COUNT(*): deduped_row_count uses visibility bitmap instead of FxHashSet - lz4_flex 0.12 -> 0.13 - Metadata decompress uses decompress_into instead of decompress_size_prepended  * feat: bounded volume compaction, streaming V4 read, target_volume_rows config  Compaction now produces balanced, target-sized volumes instead of one unbounded monolith. Volumes are split at row-group aligned boundaries (default 1M rows = 16 row groups of 64K each).  Compaction selection is content-driven: - Sub-target volumes (< target): merge together - Oversized volumes (> 150% target): split - Dirty volumes (have tombstoned rows): rewrite to reclaim dead rows - Clean at-target volumes: frozen, never rewritten  Streaming V4 read eliminates the CompressedBlockStore intermediate: blocks are read one at a time into a reusable buffer and decompressed directly into final column vectors, saving ~1.3 GB allocation throughput.  New config: target_volume_rows (default 1,048,576) configurable via DSN parameter, PRAGMA, and builder API. Minimum: 65,536 (one row group).  * refactor: remove legacy STVL/STVZ format, V1-V5 manifest, dead compaction code  Remove ~1000 lines of dead legacy code: - STVL/STVZ volume format: serialize/deserialize/constants/tests removed.   All volumes now use V4 (STV4) format with compress flag for raw blocks. - Manifest V1-V5 readers: only V6 accepted, old versions rejected. - should_compact() gate: redundant with compact_volumes() trigger logic. - compaction_epoch field on MVCCEngine: removed. SegmentMeta field kept   for V6 binary compat but always writes 0 (remove on V7 bump). - last_compaction_nanos/compaction_baseline: dead fields, never read.  Behavioral fixes: - PRAGMA RESTORE without timestamp now reads manifest-*.json to filter   eligible tables, preventing dropped tables from being resurrected. - write_volume_to_disk_opts returns CompressedBlockStore directly (not   Option), since V4 is always used. - Snapshot backup writes V4 format instead of STVL. - PRAGMA checkpoint_on_close removed (DSN-only config, not runtime). - PRAGMA target_volume_rows documented with syntax and validation.  Documentation audit: fixed ~30 inaccuracies across all doc files, README, CLI help, code comments, and memory files. Key corrections: - V4 manifest -> V6 manifest references - adaptive 4-phase -> bounded volume compaction - compact_threshold counts sub-target volumes only (min 2) - sync_interval_ms default 10 -> 1000 - compression alias sets both WAL and volume compression - --restore requires DB to open; --reset-volumes --restore for corruption - ddl.bin -> per-timestamp ddl-{timestamp}.bin + manifest-{timestamp}.json - checkpoint cycle: WAL truncation before compaction, not after - close() seals to volumes, not creates snapshot  * feat: tiered volume eviction, deferred column loading, multi-window expressions  Volume storage: - Refactor FrozenVolume into VolumeMeta (Arc-shared) + LazyColumns (swappable) - Three-tier eviction: hot (decompressed) -> warm (compressed in RAM) -> cold (metadata only, reload from disk) - Deferred column loading on read_volume_v4: blocks stay compressed until first scan - Lazy volume loading in scan paths: prune by zone maps/bloom on metadata, load cold on demand - Schema-evolution safe aggregate stats pushdown (per-volume physical column index) - Fix stats fast path to guard against overlapping row_ids after UPDATE + seal  Window functions: - Support multiple window functions in a single expression (e.g. ROW_NUMBER() + SUM() OVER()) - Support complex PARTITION BY expressions (e.g. TIME_TRUNC, arithmetic) - Fix streaming LIMIT pushdown: disable when top-level ORDER BY is present - Compute all window functions per partition (not just the first one) - Deeper expression tree traversal for window detection and classification  * feat: PRAGMA VOLUME_STATS, DML zone-map pruning, memory accounting fixes  PRAGMA VOLUME_STATS: per-table per-volume diagnostics showing tier (hot/warm/cold), row count, memory bytes, idle cycles, and tombstones.  DML zone-map pruning: UPDATE and DELETE now use get_volumes_newest_first_lazy() with zone-map and bloom filter pruning. Cold volumes are loaded on demand only after pruning confirms they may contain matching rows. Previously both paths loaded all volumes eagerly, causing unnecessary hot-tier memory spikes.  Memory accounting: FrozenVolume::memory_size() now includes VolumeMeta (zone maps, bloom filters, stats, row group metadata) via new VolumeMeta::memory_size() and ColumnBloomFilter::memory_size(). Previously only compressed store + row_ids were counted.  visibility_seen shrink: the reusable FxHashSet for bitmap computation now shrinks after use when capacity far exceeds the current total row count. Prevents ~400MB retention from a one-time large computation at startup.  * fix: window RANGE frame semantics, CHECK constraint enforcement, ROLLUP FILTER, LIMIT+OFFSET  - Default window frame (ORDER BY without explicit frame) now correctly uses   RANGE semantics: peer rows with equal ORDER BY values share the same   aggregate result (SUM, LAST_VALUE, FIRST_VALUE, etc.) - UPDATE now enforces CHECK constraints via precompiled expressions - Fast-path UPDATE bails out to normal path when CHECK constraints exist - ROLLUP/CUBE now respects aggregate FILTER clauses in both grand total   and partial grouping paths - Window LIMIT pushdown disabled when OFFSET is present - O(n) incremental accumulation for aggregate window functions with   UNBOUNDED PRECEDING start (was O(n^2) reset-per-row) - checkpoint_interval=0 documented as \"disabled\"; non-zero clamped to min 5s  * feat: cutoff-filtered seal and compaction during snapshot transactions  Replace the global seal/compaction block during snapshot isolation with a cutoff-based approach that allows progress while preserving MVCC correctness.  Seal: only extracts rows committed before the earliest snapshot's begin_seq. Post-snapshot rows stay in hot where MVCC handles visibility.  Compaction: volumes stamped with seal_seq (repurposed dead compaction_epoch field, binary-compatible). Only volumes with seal_seq <= min_snap_begin_seq are compacted. Tombstone application and cleanup filtered by commit_seq to preserve post-snapshot tombstones for correct snapshot reads.  Previously a single long-running snapshot transaction blocked all seal and compaction globally, causing unbounded hot buffer and WAL growth.  * fix: SUM precision for large integers, NaN zone map, read_buf allocation  - Volume aggregate stats: accumulate i128 integer and f64 float sums   separately across volumes to avoid per-volume precision loss from   i128→f64 conversion. Single final conversion is exact for sums   fitting in 53-bit mantissa. - Skip NaN values in volume-level zone map min/max tracking. NaN   permanently corrupts zm.max (compare_floats treats NaN as greater   than all), preventing pruning on float columns with NaN values. - Remove read_buf reuse pattern in V4 volume reader that lost capacity   via std::mem::take on every block, causing ~200 unnecessary   allocations per volume read.  * fix: critical seal data loss, grouped agg, NaN stats, restore durability  - CRITICAL: extract_for_seal_with_cutoff no longer walks version chain   prev pointer. Walking past post-cutoff HEAD extracted stale data while   remove_sealed_rows deleted the newer HEAD, losing committed updates.  - Multi-column GROUP BY deferred aggregation now bails out to regular   executor. ValueMap keyed by first column only was merging groups that   shared the first column but differed in others.  - NaN values skipped in volume aggregate stats (sum_float, min, max).   Previously poisoned stats permanently, corrupting SUM/AVG/MAX.  - Seal cutoff re-checked per-table to close TOCTOU window between   top-of-function snapshot check and per-table extraction.  - PRAGMA RESTORE returns error if post-restore checkpoint fails   (WAL already truncated, data non-durable without successful checkpoint).  - CHECK truthiness consistent across INSERT, UPDATE, and upsert:   Timestamp/Extension/other non-null types are truthy (was _ => false).  - Window incremental path excludes RANGE FOLLOWING (NULL ORDER BY   values caused non-monotonic frame_end via partition_len fallback).  - WAL sync_interval_ms now used instead of hardcoded 1 second.  - Snapshot manifest JSON escapes table names with backslash/quote.   CLI restore parser handles escaped JSON strings correctly.  - CLI --restore checks ddl-{ts}.bin exists before selecting a backup   timestamp, preventing selection of incomplete backup batches.  * fix: make sync_mode and wal_flush_trigger read-only PRAGMAs (DSN-only)  These settings were captured by WALManager at startup and never re-read, so PRAGMA SET reported success but had no runtime effect. Now SET returns an error directing users to the connection string. GET still works to inspect the current value.  * perf: IN list zone-map pruning, ORDER BY PK merge, MIN/MAX typed scan, OFFSET skip  IN list: InListExpr now returns Gte(min)/Lte(max) bounds from collect_comparisons(), enabling zone-map pruning and binary search narrowing on cold volumes. WHERE id IN (1,2,3,4,5) on 14M rows: 982ms -> 2.5ms (400x).  ORDER BY PK + LIMIT: k-way merge across volumes' sorted row_ids + hot B-tree, stopping after limit rows. Bails out for snapshot isolation. Hot side limited to needed rows (not all). ORDER BY id LIMIT 10 on 14M rows: 2.0s -> 3.7ms (540x).  MIN/MAX: typed direct f64/i64/timestamp access in fallback scan path (avoids Value allocation per row). Zone-map volume pruning skips volumes whose min > current best (for MIN). NaN correctly skipped in float paths. MIN(id),MAX(id) on 14M rows: 180ms -> 18.5ms (10x).  OFFSET: cold scan skips row materialization for offset rows when no WHERE clause is present. LIMIT 100 OFFSET 1M: 51ms -> 1.5ms (34x).  * feat: columnar aggregate pushdown, dictionary DISTINCT, typed GROUP BY  Tier 2: Filtered aggregates (WHERE + SUM/COUNT/MIN/MAX/AVG) computed directly on raw i64/f64 arrays with zero Value/Row construction. Typed predicates on integer, float, and dictionary-equality columns. Zone-map pruning with correct Value::Timestamp probes at both volume and row-group levels. DictEq resolution uses get_column_dictionary() to avoid full column decompression.  Tier 3: Columnar GROUP BY on single column. Dictionary columns use dict_id-indexed accumulator arrays (zero hashing). Int64/Timestamp columns use FxHashMap<i64, accums>. Schema-evolved default columns handled with visible-row guard to prevent spurious empty groups. Timestamp default values correctly accumulated for MIN/MAX.  Dictionary DISTINCT: SELECT DISTINCT col extracts unique values from per-volume dictionary metadata without scanning rows. Falls back to row scan for non-dictionary columns or when tombstones exist. Uses column mapping for schema evolution correctness.  Expression trait: added is_conjunctive_simple() to gate pushdown safety. IN lists return false (their collect_comparisons returns approximate bounds, not exact predicates).  * perf: parallel cold volume scanning with pre-prune and threshold guards  Parallelize collect_cold_rows, compute_filtered_aggregates, and compute_grouped_aggregates using rayon par_iter when the parallel feature is enabled (default).  Threshold guard: only dispatch to rayon when volumes >= 4 AND total cold rows >= 100K. Below that, sequential iteration avoids rayon scheduling overhead (~50us per task) that dominates small queries.  Pre-prune: filter volumes using zone-map metadata BEFORE parallel dispatch. Only applied to identity-mapped volumes (no schema evolution) and non-timestamp predicates (avoid i64/Timestamp type mismatch in Value::compare). Schema-evolved volumes pruned inside the per-volume closure after proper mapping resolution.  Grouped aggregation: sequential path merges per-volume maps immediately (O(1) extra maps). Parallel path collects all per-volume maps then merges (O(volumes) peak memory, acceptable trade-off for throughput).  collect_cold_rows pre-prune also skips volumes when WHERE predicates eliminate them via zone-map bounds, reducing rayon dispatch further.  * docs: update persistence and storage engine docs for v0.4.0 features  - Columnar aggregate pushdown (filtered and grouped) - Dictionary DISTINCT extraction - ORDER BY PK k-way merge - IN list zone-map pruning - Parallel cold volume scanning - OFFSET skip materialization - Cutoff-filtered seal/compaction during snapshot isolation - WAL sync_mode and wal_flush_trigger as DSN-only settings - Full configuration parameter table with runtime vs DSN-only column  * fix: stats fast paths use segments_raw instead of loading cold volumes  SUM/MIN/MAX stats fast paths now use segments_raw() (metadata-only Arc clone) instead of get_volumes_newest_first() which triggers cold volume reload and marks all volumes as accessed. Restores the metadata-only behavior for aggregate queries without WHERE on large cold tables.  Column resolution still uses get_volume_mapping for RENAME support.  Also: aggregate fast paths use column mapping for RENAME COLUMN correctness, updated known-limitations docs for v0.4.0.  * fix: calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX  - INTERVAL month/year arithmetic now uses calendar-aware logic (leap years,   variable month lengths) instead of 30-day/365-day approximations, matching   DATE_ADD behavior and preserving nanosecond precision - ALTER TABLE MODIFY COLUMN NOT NULL validates existing data with a streaming   IS NULL filter (limit 1 early exit) before applying the constraint - validate_cold_unique and populate_index_from_cold now use column mapping   to correctly translate schema indices to physical volume indices after   DROP COLUMN + ADD COLUMN schema evolution - Update known limitations docs to reflect fixes  * fix: resolve PK column through mapping in select_as_of cold dedup  The AS OF query path used the PK's schema position directly as a physical volume index when deduplicating cold rows against hot PKs. After DROP COLUMN before the PK column, the schema position shifts but the volume layout does not, causing wrong column reads. Now resolves through get_volume_mapping like all other cold column accesses.  * fix: reject PK updates, recursive ON UPDATE CASCADE, referenced-column cascade  - Reject UPDATE on primary key columns in both executor and Transaction   API paths. Delegate UPDATE/DELETE from Transaction::execute_statement   to the executor so all validation runs through a single path. - Recursive ON UPDATE CASCADE with depth limiting. SET NULL arm recurses   through descendants so deeper RESTRICT checks are enforced. Early exit   when child table has no matching rows avoids false depth-limit errors. - Generalize FK cascade from PK-only to any referenced UNIQUE column.   Pre-partition FKs by referenced column index so multi-column updates   dispatch each change to the correct child FKs only. - Pre-check RESTRICT constraints and depth limits before writing parent   rows via pre_check_restrict_for_update. Metadata-only gate skips the   scan for pure CASCADE trees. Apply both storage and in-memory WHERE   filters to avoid false rejections on no-op updates. - Correlated-update path captures old/new referenced-column values for   FK cascade and feeds updated rows into FK parent-existence checks. - Fix stale group cache in volume scanner causing index-out-of-bounds   panic at row-group boundaries when compressed store is unavailable.  * docs: remove fixed and unverifiable limitations from known-limitations  Remove items that were fixed this session (calendar-aware INTERVAL, MODIFY COLUMN NOT NULL validation, schema evolution CREATE INDEX, recursive ON UPDATE CASCADE) and items that could not be reproduced with the current MVCC (concurrent PK inserts, multi-table partial commit). PK uniqueness is checked at INSERT time and pessimistic row locks prevent write-write conflicts at UPDATE time.  * fix: embed build timestamp in version_info output  * fix: compaction TOCTOU, manifest truncation errors, volume corruption guards  - Move compact_seal_seq_limit capture inside per-table loop to close   TOCTOU window where a snapshot starting between tables could lose   visibility of tombstoned rows (matches seal's per-table pattern). - Return errors instead of silent break on truncated tombstones,   column renames, and dropped columns during manifest deserialization. - Use crate::core::Error::internal for manifest dir fsync errors   (consistency with all other error paths in the same function). - Validate dictionary IDs at deserialization: reject volumes where any   non-null ID >= dictionary length (prevents panic on corruption). - Validate bytes column offsets at deserialization: check overflow and   bounds (prevents panic on corrupted offset values). - Fix bytes block serialization storing original length instead of   actual copied length when data is truncated. - Guard stats accumulate against NaN float values to prevent sum   poisoning if called outside the writer's NaN-filtered path.  * fix: reject invalid DSN parameter values, ensure db cleanup on CLI error  - DSN numeric parameters (checkpoint_interval, compact_threshold, etc.)   now return an error on invalid values instead of silently using defaults. - CLI error paths drop the database before process::exit so close_engine   runs and WAL is properly flushed.  * docs: fix 9 doc-vs-code audit findings  - Remove phantom [migration] log messages that don't exist in code - Remove snapshot_compression from Removed table (it is a renamed alias) - Add transaction restriction note to PRAGMA vacuum - Fix function count from 131+ to 129 - Remove duplicate target_volume_rows row, add missing Runtime column - Add \"off\" alias to sync_mode documentation - Document sync_interval as alias for sync_interval_ms - Add snapshot_compression legacy alias to persistence.md - Remove version from benchmark title (scores not re-run for v0.4.0)  * fix: suppress unused warnings on no-default-features and wasm builds  - Prefix total_cold_rows with underscore (only used in parallel feature) - Gate eviction_epoch field and evict_idle_volumes method with   cfg(not(wasm32)) since they are only called from spawn_compaction - Rebuild wasm binary  * fix: aggregation pushdown, scanner column pruning, seal race, corruption guards  - Bail from filtered aggregation pushdown when WHERE is only partially   pushed to storage (needs_memory_filter=true). The residual predicate   was silently ignored, producing wrong aggregate results. - Clear needed_cols when collect_column_indices fails so the scanner   materializes all columns instead of reading Null for filter columns. - Replace has_row_id() point lookups with collect_hot_row_ids_into in   collect_rows_with_limit to prevent the seal race (same pattern as   scan() and collect_all_rows()). - Add offset validation in deserialize_column_block_into for Bytes:   check offset_count == row_count, overflow, and bounds. - Add bounds check for mapping.sources[col_idx] in   collect_rows_grouped_by_partition (prevents panic after ADD COLUMN). - Fix eviction_epoch off-by-one: fetch_add returns old value, use +1.  * fix: use snapshotted cs.mapping instead of live get_volume_mapping lookup  Use the pre-computed mapping from ColdSegment instead of looking up via get_volume_mapping during partition grouping. The snapshotted mapping is consistent within a query (immune to compaction removing segments mid-iteration), while the live lookup can race with background compaction returning empty mappings for removed segments.") | last monthApr 1, 2026 |
| [codecov.yml](https://github.com/stoolap/stoolap/blob/main/codecov.yml "codecov.yml") | [codecov.yml](https://github.com/stoolap/stoolap/blob/main/codecov.yml "codecov.yml") | [perf: Cow-based VM execution, function pushdown, and storage optimiza…](https://github.com/stoolap/stoolap/commit/207f95fb803a9ff142b99fbd1ebf1148750da66a "perf: Cow-based VM execution, function pushdown, and storage optimizations  - Add execute_cow() using Cow-based stack for zero-copy value handling - Implement FunctionRule to push LENGTH/UPPER/LOWER expressions to storage - Add thread-local regex caching in compiled.rs and like.rs - Add ASCII fast path for LENGTH operations (O(1) vs O(n)) - Add O(1) primary key lookup fast path in MVCC table scans - Convert UPPER/LOWER(col) LIKE patterns to case-insensitive LIKE - Add thread-local argument buffer for function evaluation - Fix operator flip bug for reversed comparisons (e.g., 5 < LENGTH(name)) - Fix Unicode case comparison in UPPER/LOWER equality checks") | 5 months agoDec 24, 2025 |
| [logo.svg](https://github.com/stoolap/stoolap/blob/main/logo.svg "logo.svg") | [logo.svg](https://github.com/stoolap/stoolap/blob/main/logo.svg "logo.svg") | [redesign: replace logo with modern ribbon shape](https://github.com/stoolap/stoolap/commit/704ac314694b4da15dac869d5c607110f9b2ff09 "redesign: replace logo with modern ribbon shape  Replace the old 3D cube icon with a flowing ribbon loop design. Keep the same green color palette (#659833, #406021, #95b746) and dark/light mode text support.") | last monthApr 11, 2026 |
| View all files |

## Repository files navigation

- [README](https://github.com/stoolap/stoolap#)
- [Contributing](https://github.com/stoolap/stoolap#)
- [Apache-2.0 license](https://github.com/stoolap/stoolap#)

[![Stoolap Logo](https://github.com/stoolap/stoolap/raw/main/logo.svg)](https://github.com/stoolap/stoolap/blob/main/logo.svg)

### A Modern Embedded SQL Database in Pure Rust

[Permalink: A Modern Embedded SQL Database in Pure Rust](https://github.com/stoolap/stoolap#a-modern-embedded-sql-database-in-pure-rust)

[Documentation](https://stoolap.io/docs) •
[Playground](https://stoolap.io/playground) •
[Releases](https://github.com/stoolap/stoolap/releases) •
[Benchmarks](https://github.com/stoolap/stoolap/blob/main/BENCHMARKS.md)

[![CI](https://github.com/stoolap/stoolap/actions/workflows/ci.yml/badge.svg)](https://github.com/stoolap/stoolap/actions/workflows/ci.yml)[![codecov](https://camo.githubusercontent.com/31ab05b12f2ffd875cae0a27cf33383e7f0e9ae6e912190d06969b816bfc0402/68747470733a2f2f636f6465636f762e696f2f67682f73746f6f6c61702f73746f6f6c61702f6272616e63682f6d61696e2f67726170682f62616467652e737667)](https://codecov.io/gh/stoolap/stoolap)[![Crates.io](https://camo.githubusercontent.com/63bc64a51ac649acdd8e671e9fae530c9a85a58adb4f988eb4774993ac89dfae/68747470733a2f2f696d672e736869656c64732e696f2f6372617465732f762f73746f6f6c61702e737667)](https://crates.io/crates/stoolap)[![GitHub release](https://camo.githubusercontent.com/bfc5aa6bd654da952349d063220d5b6c5ef19eba0f89918faf8d02fc3ef175fd/68747470733a2f2f696d672e736869656c64732e696f2f6769746875622f762f72656c656173652f73746f6f6c61702f73746f6f6c6170)](https://github.com/stoolap/stoolap/releases)[![License](https://camo.githubusercontent.com/b29de0acdfd19013f1f02689b15c933e4a6c145be9efa718288f88ba3280b1c5/68747470733a2f2f696d672e736869656c64732e696f2f62616467652f6c6963656e73652d417061636865253230322e302d626c75652e737667)](https://github.com/stoolap/stoolap/blob/main/LICENSE)

* * *

Stoolap is a feature-rich embedded SQL database built in pure Rust.
It targets low-latency transactional workloads and real-time analytical queries, with modern SQL features and no external server process.

## Why Stoolap?

[Permalink: Why Stoolap?](https://github.com/stoolap/stoolap#why-stoolap)

Stoolap is designed around practical embedded database needs:

- **ACID + MVCC**: concurrent reads and writes with transaction isolation
- **Cost-based optimization**: statistics-aware planning with adaptive execution
- **Rich SQL surface**: joins, subqueries, CTEs, window functions, advanced aggregations
- **Multiple index types**: B-tree, Hash, Bitmap, multi-column, and HNSW for vectors
- **Pure Rust runtime**: memory-safe implementation, no C/C++ dependency chain

### Feature Snapshot

[Permalink: Feature Snapshot](https://github.com/stoolap/stoolap#feature-snapshot)

| Feature | Stoolap | SQLite | DuckDB | PostgreSQL |
| --- | :-: | :-: | :-: | :-: |
| AS OF Time-Travel Queries | ✅ | ❌ | ❌ | ❌\* |
| MVCC Transactions | ✅ | ❌ | ✅ | ✅ |
| Hot/Cold Columnar Storage | ✅ | ❌ | ✅ | ❌ |
| Cost-Based Optimizer | ✅ | ❌ | ✅ | ✅ |
| Adaptive Query Execution | ✅ | ❌ | ❌ | ❌ |
| Semantic Query Caching | ✅ | ❌ | ❌ | ❌ |
| Parallel Query Execution | ✅ | ❌ | ✅ | ✅ |
| Native Vector / HNSW Search | ✅ | ❌ | ❌ | ❌ |
| Pure Rust (Memory Safe) | ✅ | ❌ | ❌ | ❌ |

\*PostgreSQL typically needs extensions for temporal query workflows.

## Quick Start

[Permalink: Quick Start](https://github.com/stoolap/stoolap#quick-start)

### Installation

[Permalink: Installation](https://github.com/stoolap/stoolap#installation)

```
[dependencies]
stoolap = "0.4"
```

Build from source:

```
git clone https://github.com/stoolap/stoolap.git
cd stoolap
cargo build --release
```

### Rust API

[Permalink: Rust API](https://github.com/stoolap/stoolap#rust-api)

```
use stoolap::Database;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    let db = Database::open_in_memory()?;

    db.execute(
        "CREATE TABLE users (
            id INTEGER PRIMARY KEY,
            name TEXT NOT NULL,
            email TEXT UNIQUE,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )",
        (),
    )?;

    db.execute(
        "INSERT INTO users (id, name, email) VALUES ($1, $2, $3)",
        (1, "Alice", "alice@example.com"),
    )?;

    for row in db.query("SELECT id, name, email FROM users WHERE id = $1", (1,))? {
        let row = row?;
        println!(
            "id={} name={} email={}",
            row.get::<i64>(0)?,
            row.get::<String>(1)?,
            row.get::<String>(2)?
        );
    }

    Ok(())
}
```

### CLI

[Permalink: CLI](https://github.com/stoolap/stoolap#cli)

```
# Interactive REPL
./stoolap

# Execute a single query
./stoolap -e "SELECT version()"

# Persistent database
./stoolap --db "file://./mydb"
```

## Stoolap Studio

[Permalink: Stoolap Studio](https://github.com/stoolap/stoolap#stoolap-studio)

[Stoolap Studio](https://github.com/stoolap/stoolap-studio) is a web-based database management interface with a SQL editor, schema browser, interactive data grid, vector search, and backup/restore.

[![Stoolap Studio](https://github.com/stoolap/stoolap/raw/main/docs/assets/img/studio/studio-light.png#gh-light-mode-only)](https://github.com/stoolap/stoolap/blob/main/docs/assets/img/studio/studio-light.png#gh-light-mode-only)

[![Stoolap Studio](https://github.com/stoolap/stoolap/raw/main/docs/assets/img/studio/studio-dark.png#gh-dark-mode-only)](https://github.com/stoolap/stoolap/blob/main/docs/assets/img/studio/studio-dark.png#gh-dark-mode-only)

```
git clone https://github.com/stoolap/stoolap-studio.git
cd stoolap-studio && npm install && npm run dev
```

## Core SQL Capabilities

[Permalink: Core SQL Capabilities](https://github.com/stoolap/stoolap#core-sql-capabilities)

### Transactions and Time-Travel

[Permalink: Transactions and Time-Travel](https://github.com/stoolap/stoolap#transactions-and-time-travel)

```
BEGIN;
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
UPDATE accounts SET balance = balance + 100 WHERE id = 2;
COMMIT;

SELECT * FROM accounts AS OF TIMESTAMP '2024-01-15 10:30:00';
SELECT * FROM inventory AS OF TRANSACTION 1234;
```

### Cost-Based Query Optimizer

[Permalink: Cost-Based Query Optimizer](https://github.com/stoolap/stoolap#cost-based-query-optimizer)

```
ANALYZE orders;
ANALYZE customers;

EXPLAIN SELECT * FROM orders WHERE customer_id = 100;

EXPLAIN ANALYZE
SELECT o.*, c.name
FROM orders o
JOIN customers c ON o.customer_id = c.id
WHERE c.country = 'US';
```

### Indexing

[Permalink: Indexing](https://github.com/stoolap/stoolap#indexing)

```
-- Auto-selected by data type
CREATE INDEX idx_created_at ON orders(created_at);   -- B-tree
CREATE INDEX idx_email ON users(email);              -- Hash
CREATE INDEX idx_active ON users(is_active) USING BITMAP;

-- Multi-column
CREATE INDEX idx_lookup ON events(user_id, event_type);
```

### Advanced SQL

[Permalink: Advanced SQL](https://github.com/stoolap/stoolap#advanced-sql)

```
WITH ranked AS (
    SELECT
        customer_id,
        amount,
        ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY amount DESC) AS rn
    FROM orders
)
SELECT * FROM ranked WHERE rn = 1;
```

## Vector and Semantic Search

[Permalink: Vector and Semantic Search](https://github.com/stoolap/stoolap#vector-and-semantic-search)

Stoolap supports native vectors via `VECTOR(N)` and approximate nearest-neighbor search with HNSW.

```
CREATE TABLE embeddings (
    id INTEGER PRIMARY KEY,
    content TEXT,
    embedding VECTOR(384)
);

CREATE INDEX idx_emb ON embeddings(embedding)
USING HNSW WITH (metric = 'cosine', m = 32, ef_construction = 400, ef_search = 128);

SELECT id, content,
       VEC_DISTANCE_COSINE(embedding, '[0.1, 0.2, ...]') AS dist
FROM embeddings
ORDER BY dist
LIMIT 10;
```

For built-in semantic text embeddings, enable the `semantic` feature:

```
[dependencies]
stoolap = { version = "0.4", features = ["semantic"] }
```

```
SELECT EMBED('How to reset my password');
```

See [Vector Search](https://stoolap.io/docs/data-types/vector-search/) and [Semantic Search](https://stoolap.io/docs/data-types/semantic-search/) docs for full workflows.

## Storage and Durability

[Permalink: Storage and Durability](https://github.com/stoolap/stoolap#storage-and-durability)

Stoolap uses a hot/cold volume storage architecture inspired by Apache Iceberg and Delta Lake:

- **Hot buffer**: in-memory MVCC store with WAL for active writes
- **Cold volumes**: immutable columnar files with zone maps, bloom filters, dictionary encoding, and LZ4 compression
- **Adaptive compaction**: background thread merges cold volumes with size-aware, fully dynamic merge strategy
- **Crash recovery**: atomic manifest writes, fsync-before-rename, WAL replay from checkpoint LSN
- **Column pruning**: cold scans materialize only columns referenced by filters and projections

```
file://./mydb?sync_mode=normal&compression=on&checkpoint_interval=60
```

| DSN Parameter | Default | Description |
| --- | --- | --- |
| `sync_mode` | `normal` | `none` (no fsync, data durable at checkpoint), `normal` (fsync every 1s), `full` (fsync every write) |
| `compression` | `on` | LZ4 for both WAL and volumes |
| `wal_compression` | `on` | LZ4 for WAL entries only |
| `volume_compression` | `on` | LZ4 for cold volume files only |
| `checkpoint_interval` | `60` | Seconds between checkpoint cycles |
| `compact_threshold` | `4` | Sub-target volumes per table before merging |
| `target_volume_rows` | `1048576` | Target rows per cold volume |

## Performance

[Permalink: Performance](https://github.com/stoolap/stoolap#performance)

Detailed benchmark results are in [BENCHMARKS.md](https://github.com/stoolap/stoolap/blob/main/BENCHMARKS.md).

Benchmark figures are point-in-time and workload-dependent. Validate on your own hardware, data distribution, and query patterns.

## Documentation

[Permalink: Documentation](https://github.com/stoolap/stoolap#documentation)

- Installation: [https://stoolap.io/docs/getting-started/installation/](https://stoolap.io/docs/getting-started/installation/)
- SQL commands: [https://stoolap.io/docs/sql-commands/sql-commands/](https://stoolap.io/docs/sql-commands/sql-commands/)
- Data types: [https://stoolap.io/docs/data-types/data-types/](https://stoolap.io/docs/data-types/data-types/)
- Functions: [https://stoolap.io/docs/functions/sql-functions-reference/](https://stoolap.io/docs/functions/sql-functions-reference/)
- Architecture: [https://stoolap.io/docs/architecture/architecture/](https://stoolap.io/docs/architecture/architecture/)
- Drivers: [Node.js](https://stoolap.io/docs/drivers/nodejs/) \| [Python](https://stoolap.io/docs/drivers/python/) \| [PHP](https://stoolap.io/docs/drivers/php/) \| [WASM](https://stoolap.io/docs/drivers/wasm/) \| [C](https://stoolap.io/docs/drivers/c/)

## Development

[Permalink: Development](https://github.com/stoolap/stoolap#development)

```
cargo build
cargo nextest run
cargo clippy --all-targets --all-features -- -D warnings
cargo fmt --check
```

## Contributing

[Permalink: Contributing](https://github.com/stoolap/stoolap#contributing)

See [CONTRIBUTING.md](https://github.com/stoolap/stoolap/blob/main/CONTRIBUTING.md).

## License

[Permalink: License](https://github.com/stoolap/stoolap#license)

Apache License 2.0. See [LICENSE](https://github.com/stoolap/stoolap/blob/main/LICENSE).

## About

A Modern Embedded SQL Database written in Rust


[stoolap.io](https://stoolap.io/ "https://stoolap.io")

### Topics

[rust](https://github.com/topics/rust "Topic: rust") [sql](https://github.com/topics/sql "Topic: sql") [database](https://github.com/topics/database "Topic: database") [mvcc](https://github.com/topics/mvcc "Topic: mvcc") [embedded-sql](https://github.com/topics/embedded-sql "Topic: embedded-sql")

### Resources

[Readme](https://github.com/stoolap/stoolap#readme-ov-file)

### License

[Apache-2.0 license](https://github.com/stoolap/stoolap#Apache-2.0-1-ov-file)

### Contributing

[Contributing](https://github.com/stoolap/stoolap#contributing-ov-file)

### Uh oh!

There was an error while loading. [Please reload this page](https://github.com/stoolap/stoolap).

[Activity](https://github.com/stoolap/stoolap/activity)

[Custom properties](https://github.com/stoolap/stoolap/custom-properties)

### Stars

[**1.2k**\\
stars](https://github.com/stoolap/stoolap/stargazers)

### Watchers

[**14**\\
watching](https://github.com/stoolap/stoolap/watchers)

### Forks

[**43**\\
forks](https://github.com/stoolap/stoolap/forks)

[Report repository](https://github.com/contact/report-content?content_url=https%3A%2F%2Fgithub.com%2Fstoolap%2Fstoolap&report=stoolap+%28user%29)

## [Releases\  14](https://github.com/stoolap/stoolap/releases)

[v0.4.0\\
Latest\\
\\
on Apr 1Apr 1, 2026](https://github.com/stoolap/stoolap/releases/tag/v0.4.0)

[\+ 13 releases](https://github.com/stoolap/stoolap/releases)

## [Packages\  0](https://github.com/orgs/stoolap/packages?repo_name=stoolap)

No packages published

## [Contributors\  1](https://github.com/stoolap/stoolap/graphs/contributors)

- [![@semihalev](https://avatars.githubusercontent.com/u/539588?s=64&v=4)](https://github.com/semihalev)[**semihalev** Yasar Alev](https://github.com/semihalev)

## Languages

- [Rust99.8%](https://github.com/stoolap/stoolap/search?l=rust)
- [C0.2%](https://github.com/stoolap/stoolap/search?l=c)

## Footer

[GitHub Homepage](https://github.com/)
© 2026 GitHub, Inc.


### Footer navigation

- [Terms](https://docs.github.com/site-policy/github-terms/github-terms-of-service)
- [Privacy](https://docs.github.com/site-policy/privacy-policies/github-privacy-statement)
- [Security](https://github.com/security)
- [Status](https://www.githubstatus.com/)
- [Community](https://github.community/)
- [Docs](https://docs.github.com/)
- [Contact](https://support.github.com/?tags=dotcom-footer)
- Manage cookies

- Do not share my personal information


You can’t perform that action at this time.